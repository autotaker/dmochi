let sum : [int] -> int = 
  fun x -> 
    if x > 0 
    then x + sum (x - 1)
    else 0;;
let rand_int : [] -> int = fun () -> let r = * in r;;

let x = rand_int() in
let r = sum x in
assert(r >= x)

